import React from 'react';

function Form(props) {
    return (
        <form action="" className="wrap-form">
            <input type="text"/>
            <button>clear</button>
        </form>
    );
}

export default Form;